## PostgreSQL in docker with Multiple DBs

This demo starts Single PostgreSQL docker instance 
providing multiple databases.

```
docker-compose up -d
docker-compose down -v --rmi all --remove-orphans
```
